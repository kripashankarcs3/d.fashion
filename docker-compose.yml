services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  app:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      CLIENT_ORIGIN: http://localhost:3001
      JWT_SECRET: ${JWT_SECRET:-change-me-in-prod}
      MONGODB_URI: mongodb://mongo:27017/deestyle
      YOUCAM_API_KEY: ${YOUCAM_API_KEY}
      # AI stylist: OpenRouter's free tier, a plain outbound HTTPS call — no
      # background process, unlike OPENCODE_MODE=server. Get a free key at
      # openrouter.ai/keys and set OPENROUTER_API_KEY in the root .env.
      OPENCODE_MODE: ${OPENCODE_MODE:-openrouter}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-inclusionai/ling-3.0-flash-vl:free}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      FIREBASE_CLIENT_EMAIL: ${FIREBASE_CLIENT_EMAIL:-}
      FIREBASE_PRIVATE_KEY: ${FIREBASE_PRIVATE_KEY:-}
      # Manual-payment review: who can approve/reject at /admin/payments, and
      # (optional) who gets emailed the moment a member submits one.
      ADMIN_EMAILS: ${ADMIN_EMAILS:-}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_APP_PASSWORD: ${SMTP_APP_PASSWORD:-}
      NOTIFY_EMAIL: ${NOTIFY_EMAIL:-}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME:-}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY:-}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET:-}
    depends_on:
      - mongo

volumes:
  mongo-data:
